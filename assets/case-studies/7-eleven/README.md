# 7-Eleven case-study media

`store-manager.mp4` — the Store Manager walkthrough shown inside the browser
frame in the "UX Decision" section of `case-study-7eleven.html`.

Exported from the Figma "Store manager" frame
(`cUqzew0NaUqviN3zcVFBs7`, node `1:38`). Drop the file in this folder under
that exact name. Until it is present the frame falls back to the poster
image `assets/images/01-store-manager-dashboard.png`.
